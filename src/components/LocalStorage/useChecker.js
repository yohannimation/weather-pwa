// Check if cookies are accepted
export const checkCookiesAccepted = () => {
    
}

// Check if user coordinate are set
export const checkCoordinateIsSet = () => {
    
}

// Check if user city name are set
export const checkCityNameIsSet = () => {
    
}

// Check if user timezone are set
export const checkTimeZoneIsSet = () => {
    
}

// Check if user units are set
export const checkUnitsIsSet = () => {
    
}

// Check the last request time
// If the last request time is >= current request time + 15 it will return true (will do the request to the API)
// If the last request time is < current request time + 15 it will return false (will not do the request to the API)
// Else the last request time is null it will return null (will redirect the user to the location page)
export const checkLastRequestTime = (currentRequestTime) => {
    
}