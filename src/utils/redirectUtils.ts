export const redirectToWeather = (): void => {
    window.location.replace('/');
}

export const redirectToLocate = (): void => {
    window.location.replace('/locate');
}

export const reload = (): void => {
    window.location.reload();
}